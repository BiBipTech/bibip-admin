export enum Paths {
  Dashboard = "/dashboard",
  Users = "/users",
  Reservations = "/reservations",
  Vehicles = "/vehicles",
  Missions = "/missions",
  Calendar = "/calendar",
  CarInfo = "/car-info",
  PastMissions = "/past-missions",
  Documents = "/documents",
}
